appId: dev.harnext.desktop
productName: harnext-desktop
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - '**/node_modules/better-sqlite3/**'
win:
  executableName: harnext-desktop
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  hardenedRuntime: true # required by notarization; uses build/entitlements.mac.plist
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  # Enabled by the release workflow via `--config.mac.notarize=true`, but only
  # when the Apple credentials (APPLE_ID / APPLE_APP_SPECIFIC_PASSWORD / APPLE_TEAM_ID)
  # are present as CI secrets. Left false here so dev builds and credential-less CI
  # never attempt — and fail — notarization.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: yasha-dev1
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
snap:
  # Upload the .snap to the GitHub release (like AppImage/deb) instead of the
  # Snap Store, whose publisher needs snapcraft + store credentials that aren't
  # on the CI runners — every tagged Linux job failed at that step (#75).
  publish: github
npmRebuild: false
publish:
  provider: github
  owner: yasha-dev1
  repo: harnext-desktop
