# QA Sweep — harnext-desktop (main @ 37ddf4f)

- **Date:** 2026-06-14 · **Loop:** cron `61a00c5c` (every 10 min, session-only)
- **Driving:** headless xvfb + borrowed Electron 39.8.10 + direct CDP (no MCP)

> Earlier detailed reports were untracked artifacts and don't persist in this
> environment; findings live in GitHub issues + merged PRs. Screenshots in `assets/`.

## This iteration — sweep of the newly-merged features

`main` gained ~40 commits since the prior exhaustive sweep: CI+Vitest, editor
dropdown w/ icons (#22), app icon (#25), "New Task" sidebar (#9), push-&-open-PR
(#19), per-loop model (#20), flexible loop schedule (#21), providers refactor,
and PR-title/body generation (#40).

**Health:** typecheck ✅ · lint ✅ · vitest ✅ · build ✅

**New features driven live (no token spend) — console 0 errors / 0 exceptions:**
- `providers.models("openai")` lazy-loads `[gpt-5.3-codex, gpt-5.1, gpt-5.1-mini]`.
- "New Task" sidebar item → routes to project home (#9).
- Settings tabs (Models/Providers/General/Appearance) render; editor dropdown shows brand icons (#22).
- New Loop form renders with a **Model** section ("pinned per loop · independent of the global default", #20) and Interval/Daily/Weekly schedule (#21).
- PR-generation (#40) verified in the prior iteration (dialog pre-fills title/base/body).

**Verified non-issue:** the loop Schedule time field looked clipped ("09:00 A…") in a
screenshot, but it's a native `<input type="time">` (scrollWidth == clientWidth) — not clipped.

**New bugs found: none.** Several issues I filed (#19/#20/#21/#22/#40) are now shipped and working.

## Still open (older, not regressions)
QA-001 (remove-project by name), QA-003 (a11y on non-button click targets),
QA-013 (`isGitRepo` matches nested dirs), QA-016 (editor cross-platform),
QA-020 (loop "in 5h" rounding) — candidates for follow-up.
